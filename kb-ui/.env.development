# All backend services are now accessed through main_control_service reverse proxy.
# Only the control-plane proxy is needed (configured in vite.config.ts).
VITE_CONTROL_PLANE_API_BASE=http://localhost:8910
